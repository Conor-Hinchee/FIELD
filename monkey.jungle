project.manifest = manifest.xml

# Compile only the active source tree. Keeps _retired/ (the old SunTimes
# sunrise/sunset component and the BodyBattery component it became, both now
# replaced by IntensityMinutes) out of the build so it emits no warnings.
base.sourcePath = source
